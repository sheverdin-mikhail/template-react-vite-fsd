# FSD + React + Redux + React router + TypeScript


# For fastest creating of slice use:

```npm run generate:slice <pages|entities|features> <exampleSlice>```

# For run dev server use:

```npm run dev```